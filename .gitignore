node_modules/
dist/
.env
*.log
.DS_Store
coverage/
tmp/
uploads/

# Claude Code local state (user-specific, do not commit)
.claude/

# TypeScript incremental build cache
*.tsbuildinfo

# Model evaluation output (generated artifact)
evaluation-report.json

# Client-supplied proprietary source docs (keep out of git history)
docs/trustpoint/*.docx
docs/trustpoint/*.xlsx

# Reconciliation test corpus: real application PDFs containing special-category
# (health) personal data. NEVER commit. Local evaluation only, delete when done.
docs/trustpoint/samples/


# Local direnv / Claude tooling config (machine-specific)
.envrc

# Generated document renders — the markdown is the master, rebuild with docs/dpia/build.mjs
docs/dpia/*.generated.html
docs/dpia/.body.*
