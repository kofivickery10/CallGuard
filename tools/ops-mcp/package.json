{
  "name": "callguard-ops-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only business metrics for Claude Code sessions in the CallGuard repo (MCP server over stdio). Local only.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
