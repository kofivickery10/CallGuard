{
  "//": "Declaration-only build for the design-sync import. packages/web is an app, not a published library, so it ships no .d.ts tree; the converter needs one to extract each component's props interface. Output is gitignored and regenerated by .design-sync/prepare.sh.",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "emitDeclarationOnly": true,
    "rootDir": "../packages/web/src",
    "outDir": "../packages/web/.ds-types"
  },
  "include": ["../packages/web/src"]
}
